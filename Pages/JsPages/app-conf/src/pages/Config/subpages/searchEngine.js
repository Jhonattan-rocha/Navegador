import React from "react";
import { Input } from "../styles";

export default function SearchEngine(props){
    return (
        <div><Input type="text" placeholder="Enter search engine" /></div>
    );
}
